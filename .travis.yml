language: go

go:
  - 1.8.x
  - 1.9.x
  - 1.10.x
  - tip

install:
  - go get -t -v ./...

script:
  - go test -v -coverprofile=coverage.out ./...

after_success:
  - bash <(curl -s https://codecov.io/bash)
